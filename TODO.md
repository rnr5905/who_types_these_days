# TODO

## Future Features

- [ ] **LLM Enhancement** - Enhance transcribed text using LLM (grammar fix, formatting, etc.)
  - Will be configurable via `.env`
  - Options: OpenAI, Groq, local LLM
  - Config: `LLM_ENHANCE=true`, `LLM_BACKEND=groq`, `LLM_PROMPT="Fix grammar and punctuation"`
